def greet(name)
  "Hello, " + name
end

def calculate(x, y)
  x + y
end

puts "greet.__name__ = #{method(:greet).name}"
puts "calculate.__name__ = #{method(:calculate).name}"
puts "greet.__doc__ = #{nil}"
puts "calculate.__doc__ = #{nil}"
