# Test block iteration with each

# Test with range
puts("Range iteration:")
(1..3).each do |n|
  puts(n)
end

# Test with array
puts("Array iteration:")
arr = [10, 20, 30]
arr.each do |val|
  puts(val)
end
