# Unless with else clause
temperature = 25

unless temperature < 0
  puts "Temperature is not freezing"
else
  puts "Temperature is freezing"
end

unless temperature > 30
  puts "Temperature is comfortable"
else
  puts "Temperature is hot"
end
